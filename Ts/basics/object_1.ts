// 1. Define the blueprint (the "shape" of the object)
let user: {
    name: string;
    email: string;
    isPaid: boolean;
};

// 2. Create an object that matches the blueprint
user = {
    name: "Alex",
    email: "alex@.com",
    isPaid: true
};


export{};
